require 'spec_helper'

describe CandidatesController, :type => :controller do
  describe 'POST #create' do
    it "should create a new candidate if non-existing" do
      pending
    end

    it "should use an existing candidate if existing" do
      pending
    end

    it "save the candidate to list of interviewees for current user" do
      pending
    end
  end
end
